/**
 * Enumeration of loop types for LoopStep.
 * 
 * @enum {string}
 * @readonly
 */
const loop_types = {
  FOR: 'for',
  FOR_EACH: 'for_each',
  GENERATOR: 'generator',
  WHILE: 'while',
};

export default loop_types;
